---
title: Design
description: Why we patch stock KWin instead of forking — and how that compares to KineticWE.
---

The goal is smooth **native** tiling inside KWin — not another script hitting the
script API ceiling — without maintaining a renamed compositor fork.

## Patch, don't fork

We patch stock `kdePackages.kwin`:

- **New code** lives as normal source files under `pkgs/kwin-tiling/src/` and is
  copied into the KWin tree at build time.
- **Existing KWin files** are changed through a small `hooks.patch` (~1,000 lines,
  +552 / −33 net across 18 files) — CMake wiring, input hooks, workspace/window
  integration.

That keeps the compositor binary, protocol support, multi-monitor handling, and
Plasma integration identical to upstream. When KDE fixes a bug in KWin, you get
it automatically. On a nixpkgs bump you only re-test the patch surface, not an
entire fork.

Layout engines set relative geometry on KWin's `CustomTile` tree. KWin's own
`Tile` / `TileManager` machinery handles gaps, window geometry, and rendering —
we add layout logic on top of infrastructure that already exists. Scrolling Path A
(`Tile::m_allowOverflow`) is a small, layout-gated exception: peeking columns may
sit past the output edge, stay pinned to that output, and not paint on the
neighbour. Instant jump-scroll; no `scrollOffset` interpolation and no render-backend
fork.

### Shared layout foundation (not a mega-engine)

Layouts are **thin arrangement engines** over one vertical primitive:

- **`StackColumn`** — ordered, weighted vertical stack (insert, drag/swap,
  height fill). Used by every kind.
- **Pure math / policy headers** — `columnmath`, `columnlayoutmath`, `insertpolicy`,
  `masterstackmath`, `gridmath`, `directionmath`, `slotlist`, `movestate`,
  `leafcolumn`, `movefsm`, `sizingpolicy`, `tilingconfig`, `scrollingmove`,
  `viewportmath`, `columnwidthpresets`, `scrollingcolumn`, `engineindex`,
  `overflowmath`, `scrollingmath`, `consumeexpelmath` — tested without linking
  KWin (`nix flake check` / `tests/run.sh`).
- **`LayoutEngine` base** — zoom/monocle reflow, resize gate, no-op knobs for
  layout-specific actions.
- **Scrolling** is deliberately separate (viewport, column widths, left-merge
  consume/expel) so those quirks never branch into master/stack/grid/columns
  paths.

Columns composes `StackColumn` + `columnlayoutmath` / `insertpolicy` (equal
widths that fill the view). Drop-zone InsertAbove/InsertBelow is shared.

For rounded corners and focus outlines on frameless windows, use the separate
[`kde-rounded-corners`](https://github.com/matinlotfali/KDE-Rounded-Corners)
effect — a normal KWin plugin, not baked into the compositor. Animated tiled
reflow is the same pattern: `kwin-effects-tiling-reflow` in this flake, which
reads `WindowTilingReflowRole` hints from the patch and never puts timing in
`LayoutEngine`.

Scrolling layout is PaperWM/niri-inspired, but we are not a niri clone: KWin
tiles live in per-output coordinates, so off-viewport columns are hidden rather
than panned with a compositor camera. Feel/animation belongs in a desktop
effect (see [#6](https://github.com/luxus/kwin-tiling/issues/6)). Open/close
shaders and focus outlines already live in luxusAi (`kwin-niri-shaders` /
`kde-rounded-corners`), not in this patch. The gap list versus niri is
[Scrolling vs niri](scrolling-vs-niri).

## Compared to KineticWE

Early ideas came from
[theblackdon/kineticwe](https://gitlab.com/theblackdon/kineticwe), a fork that
proved native tiling could live inside KWin. We ported concepts and features from
that work — little of its code remains here — and took a much slimmer integration
path instead of carrying the fork.

| | KineticWE fork | kwin-tiling |
| --- | --- | --- |
| Compositor to maintain | entire KWin tree (~3,300 tracked files) | stock `kdePackages.kwin` |
| Integration surface | **123** `src/` files diverge from upstream KWin today | **66** files (**48** new + **18** hooked) |
| Changes to existing KWin | baked into the fork (hard to isolate) | **+558 / −33** lines in `hooks.patch` |
| Extra workarounds | ~20-file QPainter render path (~1.9k LOC), hand-rolled borders/rounded corners (~500 LOC), distro install scripts (~2k LOC), `kineticwe` binary rename | rounded corners via a separate KWin effect plugin |
| Plasma bumps | rebase the whole compositor | re-test the patch surface |

The tiling hooks overlap heavily — most of our 18 hooked files are among the
changes KineticWE made — but the fork also touches **100+ other** `src/` files
(render backends, OpenGL, plugins, scene graph) that we don't need. Because we
extend KWin instead of maintaining a parallel compositor, there is no second
render path, no binary rename, and no per-distro install scaffolding.

### What we dropped from the fork

- Binary rename (`kineticwe` → stock `kwin_wayland`)
- Distro install scripts and custom packaging hacks
- Hand-rolled window borders and built-in rounded corners
- QPainter backend workaround (~20 files) — we use KWin's existing render path

### What we kept or extended

Ideas and features ported from KineticWE, reimplemented on our slimmer base:

- Master-stack, stacked, and centred layout engines (plus scrolling, which we added)
- Native `TilingController` integration with KWin's move/resize/desktop signals
- Float/ignore rules, gaps, and the settings KCM
- Cross-monitor move robustness and mouse resize inside columns

Rounded corners and focus outlines stay in
[`kde-rounded-corners`](https://github.com/matinlotfali/KDE-Rounded-Corners) —
the same pattern everywhere: extend KWin, don't fork it.

## Compared to kwilt

[jtekk1/kwilt](https://github.com/jtekk1/kwilt) is a KWin *script* (KWin's
QJSEngine scripting API), not a compositor patch — a different category of
project, and a useful look at the script-API ceiling this project exists to
get past. Its model is a flat per-(output, desktop) window queue where only
the last N windows are visible and the rest are minimized ("knocked out");
config is read once at script load, and there's no persistence or test suite.

We evaluated it for ideas, not architecture — most of it doesn't transfer to a
compiled `CustomTile`-tree engine, and a few things here are already more
capable (per-app float rules and per-(output, desktop) layout memory are
persisted; kwilt's reset every script reload). What's worth taking: a sticky
master-window pin, a focus-last shortcut, optionally hiding decorations on
tiled windows, and its `autoGrid` layout — a smoothly-interpolating
2×2→2×3→3×3→3×4 grid — as a new layout kind. There's also one correctness
lesson from its resize handling: kwilt snapshots geometry at drag-start and
only updates a split when that specific edge moved. That gating is now shipped
here (`m_activeResizes` start-geometry snapshot plus a 2px-per-axis threshold
in `endResizeWindow`).

What we're not taking: kwilt's bounded-visible-window model (`dual` and
`monocle` knock out every window past a cap). This project always tiles every
window on screen — importing a minimize-on-overflow mode would fight the
smart-gaps logic and change what "tiled" means here.

Kwilt-inspired items that shipped (sticky master pin, focus-last, borderless
when tiled, AutoGrid-as-Grid, new-window placement) live under Features;
remaining polish is on the roadmap (scrolling UX).

For packaging details and source layout, see `pkgs/kwin-tiling/README.md` in the
repository.